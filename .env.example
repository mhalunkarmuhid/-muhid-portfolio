# Copy this file to .env (and add .env to .gitignore — never commit real keys).
# The chatbot backend (api/chat.js) reads this at request time. If it's
# missing, the chatbot automatically falls back to its local knowledge base
# instead of breaking.

OPENAI_API_KEY=
